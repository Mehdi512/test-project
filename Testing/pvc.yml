apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-pvc
spec:
  resources:
    requests:
      storage:  50Mi
  accessModes:
    - ReadWriteOnce
  selector:
    matchLabels:
      pv: nginx
