apiVersion: v1
kind: PersistentVolume
metadata:
  name: nginx-pv
  labels:
    pv: nginx
spec:
  capacity:
    storage: 200Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath: 
    path: /var/nginx/
