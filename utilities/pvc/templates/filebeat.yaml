---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-filebeat-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.filebeat }}
  storageClassName: {{ .Values.storageclass_name }}
