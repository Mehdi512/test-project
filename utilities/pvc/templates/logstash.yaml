---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-logstash-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.logstash }}
  storageClassName: {{ .Values.storageclass_name }}
