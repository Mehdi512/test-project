---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-rabbitmq-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.rabbitmq }}
  storageClassName: {{ .Values.storageclass_name }}
