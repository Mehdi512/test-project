---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-minio-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.minio }}
  storageClassName: {{ .Values.storageclass_name }}
