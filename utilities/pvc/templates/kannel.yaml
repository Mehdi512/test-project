---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-kannel-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.kannel }}
  storageClassName: {{ .Values.storageclass_name }}
