---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-idm-cockpit-link-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.idmcockpitlink }}
  storageClassName: {{ .Values.storageclass_name }}
