---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-alertapp-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.alertapp }}
  storageClassName: {{ .Values.storageclass_name }}
