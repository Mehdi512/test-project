---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-idm-erp-link-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.idmerplink }}
  storageClassName: {{ .Values.storageclass_name }}
