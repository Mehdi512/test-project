---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-order-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.ordermanagement }}
  storageClassName: {{ .Values.storageclass_name }}
