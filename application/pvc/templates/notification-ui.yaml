---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-notification-ui-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.notificationui }}
  storageClassName: {{ .Values.storageclass_name }}
