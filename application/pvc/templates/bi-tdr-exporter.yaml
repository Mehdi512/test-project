---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-bi-tdr-exporter-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.bitdrexporter }}
  storageClassName: {{ .Values.storageclass_name }}
