---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-otp-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.otpmanagement }}
  storageClassName: {{ .Values.storageclass_name }}
