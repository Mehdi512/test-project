---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-cell-sim-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.cellsim }}
  storageClassName: {{ .Values.storageclass_name }}
