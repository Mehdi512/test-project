---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-helper-link-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.helperlink }}
  storageClassName: {{ .Values.storageclass_name }}
