---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-idm-voucher-activator-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.idmvoucheractivator }}
  storageClassName: {{ .Values.storageclass_name }}
