---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-link-simulator-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.linksimulator }}
  storageClassName: {{ .Values.storageclass_name }}
