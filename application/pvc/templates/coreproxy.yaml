---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-coreproxy-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.coreproxy }}
  storageClassName: {{ .Values.storageclass_name }}
