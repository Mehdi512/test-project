---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-object-store-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.objectstore }}
  storageClassName: {{ .Values.storageclass_name }}
