---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-idm-pol-link-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.idmpollink }}
  storageClassName: {{ .Values.storageclass_name }}
