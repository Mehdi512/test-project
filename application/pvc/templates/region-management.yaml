---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-region-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.regionmanagement }}
  storageClassName: {{ .Values.storageclass_name }}
