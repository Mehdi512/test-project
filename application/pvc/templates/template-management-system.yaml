---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-template-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.templatemanagement }}
  storageClassName: {{ .Values.storageclass_name }}
