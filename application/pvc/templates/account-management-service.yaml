---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-account-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.accountmanagement }}
  storageClassName: {{ .Values.storageclass_name }}
