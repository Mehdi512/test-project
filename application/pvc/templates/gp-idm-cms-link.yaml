---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-idm-cms-link-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.idmcmslink }}
  storageClassName: {{ .Values.storageclass_name }}
