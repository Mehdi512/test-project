---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-kyc-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.kyc }}
  storageClassName: {{ .Values.storageclass_name }}
