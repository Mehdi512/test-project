---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-quota-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.quotamanagement }}
  storageClassName: {{ .Values.storageclass_name }}
