---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-identity-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.identitymanagement }}
  storageClassName: {{ .Values.storageclass_name }}
