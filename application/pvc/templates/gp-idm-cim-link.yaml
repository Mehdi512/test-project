---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-idm-cim-link-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.idmcimlink }}
  storageClassName: {{ .Values.storageclass_name }}
