---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-idm-bprs-link-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.idmbprslink }}
  storageClassName: {{ .Values.storageclass_name }}
