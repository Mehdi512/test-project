---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-group-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.groupmanagement }}
  storageClassName: {{ .Values.storageclass_name }}
