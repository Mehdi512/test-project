---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-biaggregator-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.biaggregator }}
  storageClassName: {{ .Values.storageclass_name }}
