---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-idm-mfs-link-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.idmmfslink }}
  storageClassName: {{ .Values.storageclass_name }}
