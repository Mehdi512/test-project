---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-businesslogic-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.businesslogic }}
  storageClassName: {{ .Values.storageclass_name }}
