---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-ldaplink-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.ldaplink }}
  storageClassName: {{ .Values.storageclass_name }}
