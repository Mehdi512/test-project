---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-reference-generator-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.referencegenerator }}
  storageClassName: {{ .Values.storageclass_name }}
