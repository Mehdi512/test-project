---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-access-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.accessmanagement }}
  storageClassName: {{ .Values.storageclass_name }}
