---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-accountlink-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.accountlink }}
  storageClassName: {{ .Values.storageclass_name }}
