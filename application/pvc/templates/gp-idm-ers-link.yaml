---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-idm-ers-link-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.idmerslink }}
  storageClassName: {{ .Values.storageclass_name }}
