---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-notification-manager-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.notificationmanager }}
  storageClassName: {{ .Values.storageclass_name }}
