---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-grn-processor-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.grnprocessor }}
  storageClassName: {{ .Values.storageclass_name }}
