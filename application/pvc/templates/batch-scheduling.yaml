---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-batch-scheduling-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.batchscheduling }}
  storageClassName: {{ .Values.storageclass_name }}
