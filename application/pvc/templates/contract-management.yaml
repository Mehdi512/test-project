---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-contract-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.storageCapacity.contractmanagement }}
  storageClassName: {{ .Values.storageclass_name }}
