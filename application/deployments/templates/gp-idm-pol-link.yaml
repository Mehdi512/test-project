---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: idm-pol-link
  namespace: {{ .Release.Namespace }}
  labels:
    app: idm-pol-link
    egressTraffic: green
spec:
  replicas: {{ index .Values.replica.idmpollink .Values.env }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
       maxSurge:  40%
       maxUnavailable: 20%
  selector:
    matchLabels:
      app: idm-pol-link
  template:
    metadata:
      labels:
        app: idm-pol-link
    spec:
      initContainers:
      - name: copy-configs
        image: {{ .Values.image.init_centos }}
        imagePullPolicy: IfNotPresent
        command: ['bash','/scripts/configs-copy']
        volumeMounts:
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-cm-main
          mountPath: /main
        - name: vol-cm-gp-idm-pol-link-main
          mountPath: /idm-pol-link-main
        - name: vol-cm-gp-idm-pol-link-copy
          mountPath: /scripts
      {{- include "hostaliases" . | indent 6 }}
      containers:
      - name: idm-pol-link
        image: {{ .Values.image.idmpollink }}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 15900
        env:
        - name: DEFAULT_TIME_ZONE
          value: "{{ .Values.timezone }}"
        - name: TZ
          value: "{{ .Values.timezone }}"
        volumeMounts:
        - name: tz-{{ .Values.country }}
          mountPath: /usr/share/zoneinfo/Asia/Dhaka
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-logs-idm-pol-link
          mountPath: {{ .Values.logs }}/gp-idm-pol-link
      volumes:
      - name: tz-{{ .Values.country }}
        hostPath:
          path: /usr/share/zoneinfo/{{ .Values.timezone }}
      - name: vol-cm-main
        configMap:
          name: cm-main
      - name: vol-cm-gp-idm-pol-link-main
        configMap:
          name: cm-gp-idm-pol-link-main
      - name: vol-logs-idm-pol-link
        persistentVolumeClaim:
          claimName: logs-idm-pol-link-{{ .Release.Namespace }}
      - name: config-dir-main
        emptyDir: {}
      - name: vol-cm-gp-idm-pol-link-copy
        configMap:
          name: cm-gp-idm-pol-link-copy
      imagePullSecrets:
      - name: {{ .Values.secret }}
