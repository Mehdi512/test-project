---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: idm-mfs-link
  namespace: {{ .Release.Namespace }}
  labels:
    app: idm-mfs-link
    egressTraffic: green
spec:
  replicas: {{ index .Values.replica.idmmfslink .Values.env }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
       maxSurge:  40%
       maxUnavailable: 20%
  selector:
    matchLabels:
      app: idm-mfs-link
  template:
    metadata:
      labels:
        app: idm-mfs-link
    spec:
      initContainers:
      - name: copy-configs
        image: {{ .Values.image.init_centos }}
        imagePullPolicy: IfNotPresent
        command: ['bash','/scripts/configs-copy']
        volumeMounts:
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-cm-main
          mountPath: /main
        - name: vol-cm-gp-idm-mfs-link-main
          mountPath: /idm-mfs-link-main
        - name: vol-cm-gp-idm-mfs-link-copy
          mountPath: /scripts
      {{- include "hostaliases" . | indent 6 }}
      containers:
      - name: idm-mfs-link
        image: {{ .Values.image.idmmfslink }}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 15600
        env:
        - name: DEFAULT_TIME_ZONE
          value: "{{ .Values.timezone }}"
        - name: TZ
          value: "{{ .Values.timezone }}"
        volumeMounts:
        - name: tz-{{ .Values.country }}
          mountPath: /usr/share/zoneinfo/Asia/Dhaka
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-logs-idm-mfs-link
          mountPath: {{ .Values.logs }}/gp-idm-mfs-link
      volumes:
      - name: tz-{{ .Values.country }}
        hostPath:
          path: /usr/share/zoneinfo/{{ .Values.timezone }}
      - name: vol-cm-main
        configMap:
          name: cm-main
      - name: vol-cm-gp-idm-mfs-link-main
        configMap:
          name: cm-gp-idm-mfs-link-main
      - name: vol-logs-idm-mfs-link
        persistentVolumeClaim:
          claimName: logs-idm-mfs-link-{{ .Release.Namespace }}
      - name: config-dir-main
        emptyDir: {}
      - name: vol-cm-gp-idm-mfs-link-copy
        configMap:
          name: cm-gp-idm-mfs-link-copy
      imagePullSecrets:
      - name: {{ .Values.secret }}
