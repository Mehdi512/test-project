---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: idm-ers-link
  namespace: {{ .Release.Namespace }}
  labels:
    app: idm-ers-link
    egressTraffic: green
spec:
  replicas: {{ index .Values.replica.idmerslink .Values.env }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
       maxSurge:  40%
       maxUnavailable: 20%
  selector:
    matchLabels:
      app: idm-ers-link
  template:
    metadata:
      labels:
        app: idm-ers-link
    spec:
      initContainers:
      - name: copy-configs
        image: {{ .Values.image.init_centos }}
        imagePullPolicy: IfNotPresent
        command: ['bash','/scripts/configs-copy']
        volumeMounts:
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-cm-main
          mountPath: /main
        - name: vol-cm-gp-idm-ers-link-main
          mountPath: /idm-ers-link-main
        - name: vol-cm-gp-idm-ers-link-copy
          mountPath: /scripts
      {{- include "hostaliases" . | indent 6 }}
      containers:
      - name: idm-ers-link
        image: {{ .Values.image.idmerslink }}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 15500
        env:
        - name: DEFAULT_TIME_ZONE
          value: "{{ .Values.timezone }}"
        - name: TZ
          value: "{{ .Values.timezone }}"
        volumeMounts:
        - name: tz-{{ .Values.country }}
          mountPath: /usr/share/zoneinfo/Asia/Dhaka
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-logs-idm-ers-link
          mountPath: {{ .Values.logs }}/gp-idm-ers-link
      volumes:
      - name: tz-{{ .Values.country }}
        hostPath:
          path: /usr/share/zoneinfo/{{ .Values.timezone }}
      - name: vol-cm-main
        configMap:
          name: cm-main
      - name: vol-cm-gp-idm-ers-link-main
        configMap:
          name: cm-gp-idm-ers-link-main
      - name: vol-logs-idm-ers-link
        persistentVolumeClaim:
          claimName: logs-idm-ers-link-{{ .Release.Namespace }}
      - name: config-dir-main
        emptyDir: {}
      - name: vol-cm-gp-idm-ers-link-copy
        configMap:
          name: cm-gp-idm-ers-link-copy
      imagePullSecrets:
      - name: {{ .Values.secret }}
