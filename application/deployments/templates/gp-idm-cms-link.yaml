---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: idm-cms-link
  namespace: {{ .Release.Namespace }}
  labels:
    app: idm-cms-link
    egressTraffic: green
spec:
  replicas: {{ index .Values.replica.idmcmslink .Values.env }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
       maxSurge:  40%
       maxUnavailable: 20%
  selector:
    matchLabels:
      app: idm-cms-link
  template:
    metadata:
      labels:
        app: idm-cms-link
    spec:
      initContainers:
      - name: copy-configs
        image: {{ .Values.image.init_centos }}
        imagePullPolicy: IfNotPresent
        command: ['bash','/scripts/configs-copy']
        volumeMounts:
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-cm-main
          mountPath: /main
        - name: vol-cm-gp-idm-cms-link-main
          mountPath: /idm-cms-link-main
        - name: vol-cm-gp-idm-cms-link-copy
          mountPath: /scripts
      {{- include "hostaliases" . | indent 6 }}
      containers:
      - name: idm-cms-link
        image: {{ .Values.image.idmcmslink }}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 15800
        env:
        - name: DEFAULT_TIME_ZONE
          value: "{{ .Values.timezone }}"
        - name: TZ
          value: "{{ .Values.timezone }}"
        volumeMounts:
        - name: tz-{{ .Values.country }}
          mountPath: /usr/share/zoneinfo/Asia/Dhaka
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-logs-idm-cms-link
          mountPath: {{ .Values.logs }}/gp-idm-cms-link
      volumes:
      - name: tz-{{ .Values.country }}
        hostPath:
          path: /usr/share/zoneinfo/{{ .Values.timezone }}
      - name: vol-cm-main
        configMap:
          name: cm-main
      - name: vol-cm-gp-idm-cms-link-main
        configMap:
          name: cm-gp-idm-cms-link-main
      - name: vol-logs-idm-cms-link
        persistentVolumeClaim:
          claimName: logs-idm-cms-link-{{ .Release.Namespace }}
      - name: config-dir-main
        emptyDir: {}
      - name: vol-cm-gp-idm-cms-link-copy
        configMap:
          name: cm-gp-idm-cms-link-copy
      imagePullSecrets:
      - name: {{ .Values.secret }}
