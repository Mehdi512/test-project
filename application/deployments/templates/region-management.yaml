---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: region-management
  namespace: {{ .Release.Namespace }}
  labels:
    app: region-management
    egressTraffic: green
spec:
  replicas: {{ index .Values.replica.regionmanagement .Values.env }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
       maxSurge:  40%
       maxUnavailable: 20%
  selector:
    matchLabels:
      app: region-management
  template:
    metadata:
      labels:
        app: region-management
    spec:
      initContainers:
      - name: copy-configs
        image: {{ .Values.image.init_centos }}
        imagePullPolicy: IfNotPresent
        command: ['bash','/scripts/configs-copy']
        volumeMounts:
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-cm-main
          mountPath: /main
        - name: vol-cm-region-management-main
          mountPath: /region-management-main
        - name: vol-cm-region-management-templates
          mountPath: /region-management-templates
        - name: vol-cm-region-management-copy
          mountPath: /scripts
      containers:
      - name: region-management
        image: {{ .Values.image.regionmanagement }}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        readinessProbe:
          tcpSocket:
            port: 8000
          {{- include "probe" (list . "readiness" "regionmanagement") | nindent 10 }}
        livenessProbe:
          tcpSocket:
            port: 8000
          {{- include "probe" (list . "liveness" "regionmanagement") | nindent 10 }}
        env:
        - name: DEFAULT_TIME_ZONE
          value: "{{ .Values.timezone }}"
        - name: TZ
          value: "{{ .Values.timezone }}"
        volumeMounts:
        - name: tz-{{ .Values.country }}
          mountPath: /usr/share/zoneinfo/Asia/Dhaka
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-logs-region-management
          mountPath: {{ .Values.logs }}/region-management-core
      volumes:
      - name: tz-{{ .Values.country }}
        hostPath:
          path: /usr/share/zoneinfo/{{ .Values.timezone }}
      - name: vol-cm-main
        configMap:
          name: cm-main
      - name: vol-cm-region-management-main
        configMap:
          name: cm-region-management-main
      - name: vol-cm-region-management-templates
        configMap:
          name: cm-region-management-templates
      - name: vol-logs-region-management
        persistentVolumeClaim:
          claimName: logs-region-management-{{ .Release.Namespace }}
      - name: config-dir-main
        emptyDir: {}
      - name: vol-cm-region-management-copy
        configMap:
          name: cm-region-management-copy
      imagePullSecrets:
      - name: {{ .Values.secret }}
